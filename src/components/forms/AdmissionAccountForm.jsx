import { useState } from 'react';import { Link } from 'react-router-dom';
export default function AdmissionAccountForm(){const [msg,setMsg]=useState('');const submit=e=>{e.preventDefault();const f=new FormData(e.target);const p=f.get('password');if(p.length<6)return setMsg('Password must be at least 6 characters.');if(p!==f.get('confirm'))return setMsg('Passwords do not match.');localStorage.setItem('admissionDemoUser',JSON.stringify(Object.fromEntries(f.entries())));setMsg('Account created successfully. You can now continue with the admission application.');};return <form onSubmit={submit} className='soft-card p-6 space-y-3'><input name='name' required placeholder='Parent/Guardian Full Name' className='w-full p-3 rounded-xl border'/><input name='phone' required placeholder='Phone Number' className='w-full p-3 rounded-xl border'/><input name='email' type='email' required placeholder='Email Address' className='w-full p-3 rounded-xl border'/><input name='password' type='password' required placeholder='Password' className='w-full p-3 rounded-xl border'/><input name='confirm' type='password' required placeholder='Confirm Password' className='w-full p-3 rounded-xl border'/><input name='relationship' required placeholder='Relationship to Child' className='w-full p-3 rounded-xl border'/><input name='classLevel' required placeholder='Preferred Class Level' className='w-full p-3 rounded-xl border'/><button className='premium-button bg-gold'>Create Account</button>{msg&&<p className='text-softGreen'>{msg}</p>}{msg&&<Link to='/admission-application' className='inline-block text-sky font-semibold'>Continue Application</Link>}</form>}
