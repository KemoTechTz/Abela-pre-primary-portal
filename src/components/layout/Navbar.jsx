import { useState } from 'react';import { NavLink, Link } from 'react-router-dom';import { Menu,X } from 'lucide-react';import { schoolConfig } from '../../data/schoolConfig';
export default function Navbar(){const [open,setOpen]=useState(false);return <header className='fixed top-0 w-full z-50 bg-navy text-white shadow'><nav className='max-w-6xl mx-auto px-4 py-3 flex items-center justify-between'><Link to='/' className='font-bold'>{schoolConfig.schoolName}</Link><div className='hidden md:flex gap-4 items-center'>{schoolConfig.navigationLinks.map(([n,p])=><NavLink key={p} to={p} className={({isActive})=>isActive?'text-gold':'text-white/90'}>{n}</NavLink>)}<Link to='/admissions' className='bg-gold text-navy px-4 py-2 rounded-full font-semibold'>Apply Now</Link></div><button className='md:hidden' onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></nav>{open&&<div className='md:hidden bg-navy border-t border-white/20 p-4 space-y-3'>{schoolConfig.navigationLinks.map(([n,p])=><NavLink key={p} onClick={()=>setOpen(false)} to={p} className='block'>{n}</NavLink>)}</div>}</header>}
